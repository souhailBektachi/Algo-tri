#include"algo_trie.h"
int main(void)
{
    // int taille=17,tab[17];
    // printf("\n test ... ");
    // random_v(tab,taille);
    // printf("\n test ... ");
    // printf("\n %d",tab[0]);
    // affiche_tab(tab,taille);
    // trie_par_selection(tab,taille);
    // affiche_tab(tab,taille);
    
    double M[2][10];
    remplir_matrice_temp(M);
    affiche_matrice(M);
}